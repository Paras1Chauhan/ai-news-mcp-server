version: '3.8'

services:
  mcp-server:
    build: .
    container_name: ai-news-mcp-server
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - PORT=3000
      - TRANSPORT=http
    restart: always